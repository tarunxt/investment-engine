map $http_upgrade $connection_upgrade {
    default upgrade;
    '' close;
}

server {
    listen 80;
    server_name alkraj.com www.alkraj.com api.alkraj.com;

    location /.well-known/acme-challenge/ {
        root /var/www/letsencrypt;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl http2;
    server_name www.alkraj.com;

    ssl_certificate /etc/letsencrypt/live/alkraj.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/alkraj.com/privkey.pem;

    return 301 https://alkraj.com$request_uri;
}

server {
    listen 443 ssl http2;
    server_name alkraj.com;

    ssl_certificate /etc/letsencrypt/live/alkraj.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/alkraj.com/privkey.pem;

    client_max_body_size 25m;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
    }
}

server {
    listen 443 ssl http2;
    server_name api.alkraj.com;

    ssl_certificate /etc/letsencrypt/live/api.alkraj.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/api.alkraj.com/privkey.pem;

    client_max_body_size 25m;
    proxy_read_timeout 3600;
    proxy_send_timeout 3600;

    location /ws/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
    }

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
    }
}
