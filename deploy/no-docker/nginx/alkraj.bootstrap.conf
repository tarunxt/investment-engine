server {
    listen 80;
    server_name alkraj.com www.alkraj.com api.alkraj.com;

    location /.well-known/acme-challenge/ {
        root /var/www/letsencrypt;
    }

    location / {
        return 200 'alkraj nginx bootstrap is ready\n';
        add_header Content-Type text/plain;
    }
}
