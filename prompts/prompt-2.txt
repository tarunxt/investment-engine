"I will paste outputs from few different LLMs. Each output contains recommendations for India aggressive swing-trade stocks based on the same original request.
LLMs used: GPT-5.4 Thinking, GPT-5.3, Gemini 3 Flash-1, Gemini 3 Flash-2, Claude Sonnet 4.6 (claude-sonnet-4-6) , DeepSeek-1, DeepSeek-2

Your job is to analyze all  outputs and produce exactly 1 Table only, in the same structure and style every time.

Strict output rules:
1. Return only Table 1.
2. Do not include any introduction, explanation, notes, assumptions, observations, bullet points, summary, or conclusion.
5. Standardize stock names so duplicates with slightly different naming are treated as the same stock.
   Examples:
   - IRM Energy = IRMENERGY
   - Piramal Pharma = PPLPHARMA
   - Godrej Industries = GODREJIND
   - Clean Science & Technology = CLEAN
6. Do not invent data. Use only values explicitly present in the pasted LLM outputs.
7. If any LLM does not provide a value for a field, write exactly: Not mentioned
8. Keep wording concise but meaningful inside cells.
9. Preserve the exact row style:
   - one row each for each LLM which has recommended that stock. Do not include rows for LLMs which do not recommend that stock.  
   - one final row called Average / Consolidated View
10. For numeric fields, calculate averages only from the LLMs that actually provided numeric values. Ignore “Not mentioned” cells in calculations.
11. For ranges:
   - Entry Range / Target: average the lower bounds and upper bounds separately, then present as one range
   - Upside Horizon: if given as “X–Y% / A–B weeks”, average the percentage bounds separately and average the week bounds separately, then present in the same format
12. For single numeric values such as Stop Loss, Units, Price Per Unit, Total Buy Amount, and Confidence Score, calculate the simple average from available numeric values only.
13. For non-numeric fields such as Technical Setup, Analyst/Source, and Rationale Cruxx:
   - show LLM-wise values in each row
   - in the final row, write a concise merged summary under Average / Consolidated View
14. In Table 1, select the Top 5 unique stock picks overall using:
   - frequency of mention across the  LLM outputs
   - strength of recommendation
   - quality and clarity of rationale
15. In Table 1, include all unique stocks that appear in the Table 1 sections across all  LLM outputs.
16. In Table 1, sort stock based on average confidence score in Average / Consolidated Rows - descending order order.
17. Maintain the same vertical format for each stock:
   - stock name row group starts
   - then LLM1 model name label  
   - then LLM2 model name label 
   - then LLM3 model name label 
   - then LLM4 model name label 
   - etc
   - then Average / Consolidated View

Table format to follow exactly:
Title:
## [TODAY'S DATE] | How to Invest INR 10,000 | Consolidated Table | Generated by [LLM Name + Model]]

Table Columns:
| Stock Name | Technical Setup | Entry Range | Stop Loss | Target | Analyst/Source | Units to Buy | Price Per Unit | Total Buy Amount | Upside Horizon (% return in weeks) | Confidence Score (0-100) | Rationale Cruxx | Score Rationale Cruxx |


Rules for Table 1:
- Include every unique stock that appears in any of the above Table 1 outputs.
- Under “Suggested by which LLM”, write the relevant LLM model name label for each row.
- Do not include rows for LLMs which do not recommend that stock.
- For the final row, write: Average / Consolidated View
- For the final row:
  - Suggested by which LLM = write only ""x""/""y"" format (where x=number of LLMs in which this stock is mentioned; y=total LLMs) 
  - Technical Setup = concise merged view
  - Units = averaged numeric value
  - Price Per Unit = averaged numeric value
  - Total Buy Amount = averaged numeric value
  - Upside Horizon = averaged % and weeks
  - Confidence Score = averaged numeric value
  - Rationale Cruxx = concise consolidated view

Important formatting rules:
- Output must be plain markdown tables only.
- No code fences in the final answer.
- No extra headings except the date line above each table.
- No numbering.
- No commentary.
- Keep the structure identical every time.
- all Consolidated rows to be in bold

Now wait for me to paste the  LLM outputs, then generate the Consolidated Table in exactly this format."