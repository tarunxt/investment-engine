name: Pull Request Checks

on:
  pull_request:

permissions:
  contents: write

jobs:
  frontend:
    name: Frontend build and dashboard regression tests
    runs-on: ubuntu-latest
    timeout-minutes: 20

    defaults:
      run:
        working-directory: frontend

    env:
      NODE_ENV: production
      NEXTAUTH_URL: http://localhost:3000
      NEXTAUTH_SECRET: pull-request-check-secret
      NEXT_PUBLIC_FRONTEND_URL: http://localhost:3000
      BACKEND_API_URL: http://127.0.0.1:8000

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 1
          show-progress: false

      - name: Apply duration popup source patch
        id: duration_patch
        working-directory: ${{ github.workspace }}
        run: |
          set +e
          python3 -u scripts/apply_duration_breakdown_fix.py > /tmp/duration-patch.log 2>&1
          status=$?
          set -e
          cat /tmp/duration-patch.log
          if [ "$status" -eq 0 ]; then
            echo "applied=true" >> "$GITHUB_OUTPUT"
          else
            git reset --hard HEAD
            cp /tmp/duration-patch.log duration-patch.log
            echo "applied=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Upload duration patch diagnostic
        if: steps.duration_patch.outputs.applied == 'false'
        uses: actions/upload-artifact@v4
        with:
          name: duration-patch-diagnostic
          path: duration-patch.log
          if-no-files-found: error

      - name: Check patch formatting
        if: steps.duration_patch.outputs.applied == 'true'
        working-directory: ${{ github.workspace }}
        run: git diff --check

      - name: Set up Node
        if: steps.duration_patch.outputs.applied == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        if: steps.duration_patch.outputs.applied == 'true'
        run: npm ci --no-audit --fund=false

      - name: Run dashboard regression tests
        if: steps.duration_patch.outputs.applied == 'true'
        run: >-
          node --test
          tests/bullpen-server-auth.test.mjs
          tests/dashboard-refresh-errors.test.mjs

      - name: Build frontend
        if: steps.duration_patch.outputs.applied == 'true'
        run: npm run build -- --webpack 2>&1 | tee frontend-build.log

      - name: Commit generated duration popup fix
        if: steps.duration_patch.outputs.applied == 'true'
        working-directory: ${{ github.workspace }}
        run: |
          git fetch --depth=1 origin main
          git checkout FETCH_HEAD -- .github/workflows/pr-checks.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Fix dashboard stage duration breakdown interaction"
          git push origin HEAD:${{ github.event.pull_request.head.ref }}

      - name: Upload frontend build log
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: frontend-build-log
          path: frontend/frontend-build.log
          if-no-files-found: ignore
